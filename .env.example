# Example environment variables for TrackUp Electron app
# Copy this file to ".env" and adjust values as needed.

# ------------------------------------------------------------
# Logger configuration
# ------------------------------------------------------------
# Comma-separated list of enabled levels (default differs dev/prod)
# Possible: log, error, warn, debug, verbose, info, success
LOG_LEVELS=log,error,warn,debug,verbose,info,success

# Display order for formatter: level-first | context-first
LOG_ORDER=level-first


# ------------------------------------------------------------
# Node environment
# ------------------------------------------------------------
# Typical values: development | production
NODE_ENV=development

# ------------------------------------------------------------
# Electron Vite (dev only)
# ------------------------------------------------------------
# Set automatically by electron-vite dev. You usually don't need to set this.
ELECTRON_RENDERER_URL=

# ------------------------------------------------------------
# Renderer (Vite) runtime config
# ------------------------------------------------------------
# If you want to configure Supabase via env instead of hardcoding,
# switch your renderer code to use import.meta.env.VITE_SUPABASE_* and set here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

